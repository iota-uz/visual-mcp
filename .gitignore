# dependencies
node_modules/

# build output
dist/
*.tsbuildinfo

# Convex agent-skill bundles installed locally by `npx convex ai-files install`
# — duplicates of skills already available globally via the Claude Code
# Convex plugin; skills-lock.json (the real config) stays tracked.
.claude/skills/
.agents/skills/

# session workspaces created at runtime (see PLAN.md section 7 / src/types.ts SESSIONS_ROOT)
sessions/

# scratch dir for local template previews
tmp/
output/
.playwright-cli/
.playwright-mcp/
examples/osago-24/preview.html

# logs
npm-debug.log*
*.log

# OS
.DS_Store

# editor
.vscode/*
!.vscode/extensions.json
.idea/

# env
.env
.env.local

# isolated local stack for headless agents (npm run dev:agent) — see AGENTS.md
.env.agent
